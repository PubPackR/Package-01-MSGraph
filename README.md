# MSGraph
To install the package in your local R run:

```
devtools::install_github("Studyflix/Package-01-MSGraph",
                          ref = "main",
                          force = TRUE)

```

# MSGraph
To install the package in your local R run:

```
devtools::install_github("PubPackR/Package-01-MSGraph",
                          ref = "master",
                          force = TRUE)

```
